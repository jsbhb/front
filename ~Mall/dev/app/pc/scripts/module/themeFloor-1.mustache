<div id="themeFloor-1-{{sign}}" v-cloak>
    <div class="themeFloor-1-content">
        <div class="themeFloor-title">
            {{own.title}}
        </div>
        <div class="goodsList">
            {{#each cont}}
                <a class="goodsItem" href="{{href}}">
                    <div class="goodsItem-db">
                        <div class="goodsImg">
                            <img src="{{picPath}}" alt="中国供销海外购-正品保障、全网低价、跨境商品、海淘、网红爆品、明星同款">
                        </div>
                        <div class="goodsName">{{title}}</div>
                        <div class="goodsDesc">{{description}}</div>
                        <div class="goodsPrice">
                            ￥
                            <span class="price">
                                {{price}}
                            </span>
                            <span class="realPrice">
                            ￥{{enname}}
                        </span>
                        </div>
                    </div>
                </a>
            {{/each}}
        </div>
    </div>
</div>